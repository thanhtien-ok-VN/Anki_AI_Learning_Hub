Generate a word matching game (English terms to Vietnamese definitions).
Topic: {topic}
Pairs: {num_pairs}

CRITICAL LOGIC RULE:
- Generate an array of flattened items. 
- Do NOT nest the term and definition. 
- Use a shared `pair_id` (e.g., "pair_1") to link the matching term and definition. 
- DO NOT include any `is_correct` flags.
- Shuffle the order of the items in the output array so terms and definitions are mixed.

Return ONLY raw JSON matching this structure:
{
  "game_id": "uuid_random_string",
  "items": [
    {"id": "item_1", "content": "Apple", "type": "term", "pair_id": "pair_1"},
    {"id": "item_2", "content": "Banana", "type": "term", "pair_id": "pair_2"},
    {"id": "item_3", "content": "Quả táo", "type": "definition", "pair_id": "pair_1"},
    {"id": "item_4", "content": "Quả chuối", "type": "definition", "pair_id": "pair_2"}
  ],
  "config": { "total_pairs": 2, "time_limit_sec": 60 },
  "metadata": { "topic": "{topic}", "level": "{level}" }
}
